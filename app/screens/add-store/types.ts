type ContentActions = {
  back: string;
  createList: string;
};

export type Suggestions = {
  stores: string[];
  misc: string[];
};

export const CONTENT_ACTIONS: ContentActions = {
  back: 'back',
  createList: 'createList',
};
