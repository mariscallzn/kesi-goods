import {ActionCallback} from '../../../../../inf/multiViewRenderer';
import {AddOrUpdateBSMetadata} from '../types';

export type AddOrUpdateListProps = {
  metadata?: AddOrUpdateBSMetadata;
  action: ActionCallback;
};
